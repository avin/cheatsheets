// ---------------------------------------------------
// 📌 Строка -> массив и обратно
// ---------------------------------------------------
const arr = str.split('');
const str2 = arr.join('');
const chars = Array.from(str);


// ---------------------------------------------------
// 📌 Поиск подстроки
// ---------------------------------------------------
str.includes('abc');     // true / false
str.indexOf('abc');      // позиция или -1


// ---------------------------------------------------
// 📌 Сравнение строк
// ---------------------------------------------------
str1 === str2;
str1.toLowerCase() === str2.toLowerCase();


// ---------------------------------------------------
// 📌 Слияние строк
// ---------------------------------------------------
const merged = str1 + str2;
const merged2 = `${str1}${str2}`;


// ---------------------------------------------------
// 📌 Удаление символов
// ---------------------------------------------------
str.replace(/x/g, '');


// ---------------------------------------------------
// 📌 Замена символов / подстрок
// ---------------------------------------------------
str.replace(/l/g, 'X');       // все 'l'
str.replace('ll', 'XX');      // только первое 'll'


// ---------------------------------------------------
// 📌 Регулярные выражения
// ---------------------------------------------------
const found = /l/.test(str);
const matches = str.match(/l/g);


// ---------------------------------------------------
// 📌 Удаление пробелов (trim)
// ---------------------------------------------------
const trimmed = str.trim();           // с обеих сторон
const leftTrim = str.trimStart();     // слева
const rightTrim = str.trimEnd();      // справа


// ---------------------------------------------------
// 📌 Проверка префикса/суффикса
// ---------------------------------------------------
const hasPrefix = str.startsWith('pre');
const hasSuffix = str.endsWith('fix');


// ---------------------------------------------------
// 📌 Разделение строки по разделителю
// ---------------------------------------------------
const parts = str.split(',');         // разделить по запятой
const words = str.split(/\s+/);       // разделить по пробелам


// ---------------------------------------------------
// 📌 Повторение строки
// ---------------------------------------------------
const repeated = 'Go'.repeat(3);      // 'GoGoGo'


// ---------------------------------------------------
// 📌 Получение кода символа и символа по коду
// ---------------------------------------------------
const code = str.charCodeAt(0);       // код первого символа
const char = String.fromCharCode(65); // 'A'


// ---------------------------------------------------
// 📌 Подстрока
// ---------------------------------------------------
const sub = str.substring(0, 5);      // [0, 5)
const sub2 = str.slice(2, 5);         // [2, 5)
const sub3 = str.slice(-3);           // последние 3 символа